# Prediction-with-Graph-Neural-Networks
This project predicts the severity of US traffic accidents, framed as a node classification problem. Each accident is represented as a node in a graph, with edges connecting geographically or state-proximate incidents. We compare four GNN models across multiple hyperparameter configurations to identify the best-performing approach.
